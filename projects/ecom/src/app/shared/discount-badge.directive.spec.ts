import { DiscountBadgeDirective } from './discount-badge.directive';

describe('DiscountBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscountBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
